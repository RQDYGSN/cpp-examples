#include <iostream>

using namespace std;

int main() {
	// cout: a global object of ostream ( typedef basic_ostream<char> ostream )
	// <<: ostream& ostream::operator<< (string v)
	// endl: '\n' + flush

	// What is stream? Serial IO interface which handles the IO data one by one.

	return 0;
}